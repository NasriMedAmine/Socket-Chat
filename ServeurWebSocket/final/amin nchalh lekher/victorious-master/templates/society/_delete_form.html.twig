<form method="post" action="{{ path('app_society_delete', {'idSociety': society.idSociety}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ society.idSociety) }}">
    <button class="btn">Delete</button>
</form>
