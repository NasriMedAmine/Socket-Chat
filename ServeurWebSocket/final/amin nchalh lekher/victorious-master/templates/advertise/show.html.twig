{% extends 'base.html.twig' %}

{% block title %}Advertise{% endblock %}

{% block body %}
    <h1>Advertise</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdAdvertise</th>
                <td>{{ advertise.idAdvertise }}</td>
            </tr>
            <tr>
                <th>NameSociety</th>
                <td>{{ advertise.nameSociety }}</td>
            </tr>
            <tr>
                <th>DateDebut</th>
                <td>{{ advertise.dateDebut ? advertise.dateDebut|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>DateFin</th>
                <td>{{ advertise.dateFin ? advertise.dateFin|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_advertise_index') }}">back to list</a>

    <a href="{{ path('app_advertise_edit', {'idAdvertise': advertise.idAdvertise}) }}">edit</a>

    {{ include('advertise/_delete_form.html.twig') }}
{% endblock %}
