<form method="post" action="{{ path('app_advertise_delete', {'idAdvertise': advertise.idAdvertise}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ advertise.idAdvertise) }}">
    <button class="btn">Delete</button>
</form>
