<form method="post" action="{{ path('app_leader_delete', {'id': leader.idLeader}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ leader.idLeader) }}">
    <button class="btn">Delete</button>
</form>
