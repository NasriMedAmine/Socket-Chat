{% extends 'base.html.twig' %}

{% block title %}Interditmot index{% endblock %}

{% block body %}
    <h1>Interditmot index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Mot</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for interditmot in interditmots %}
            <tr>
                <td>{{ interditmot.id }}</td>
                <td>{{ interditmot.mot }}</td>
                <td>
                    <a href="{{ path('app_interditmot_show', {'id': interditmot.id}) }}">show</a>
                    <a href="{{ path('app_interditmot_edit', {'id': interditmot.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_interditmot_new') }}">Create new</a>
{% endblock %}
