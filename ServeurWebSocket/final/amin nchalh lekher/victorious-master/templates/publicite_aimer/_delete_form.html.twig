<form method="post" action="{{ path('app_publicite_aimer_delete', {'idPubAimer': publicite_aimer.idPubAimer}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ publicite_aimer.idPubAimer) }}">
    <button class="btn">Delete</button>
</form>
