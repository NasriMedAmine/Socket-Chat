{% extends 'base.html.twig' %}

{% block title %}Player{% endblock %}

{% block body %}
    <h1>Player</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>id</th>
                <td>{{ player.idPlayer }}</td>
            </tr>
            <tr>
                <th>Level</th>
                <td>{{ player.level }}</td>
            </tr>
            <tr>
                <th>Pseudo</th>
                <td>{{ player.pseudo }}</td>
            </tr>
            <tr>
                <th>Teams</th>
                <td>{{ player.teams }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ player.password }}</td>
            </tr>
            <tr>
                <th>Mail</th>
                <td>{{ player.mail }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_player_index') }}">back to list</a>

    <a href="{{ path('app_player_edit', {'id': player.idPlayer}) }}">edit</a>

    {{ include('player/_delete_form.html.twig') }}
{% endblock %}
