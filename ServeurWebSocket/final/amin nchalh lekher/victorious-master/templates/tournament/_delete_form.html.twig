<form method="post" action="{{ path('app_tournament_delete', {'idTournament': tournament.idTournament}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tournament.idTournament) }}">
    <button class="btn">Delete</button>
</form>
