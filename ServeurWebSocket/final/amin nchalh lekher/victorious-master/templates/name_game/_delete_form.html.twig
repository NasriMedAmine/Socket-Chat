<form method="post" action="{{ path('app_name_game_delete', {'id': name_game.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ name_game.id) }}">
    <button class="btn">Delete</button>
</form>
