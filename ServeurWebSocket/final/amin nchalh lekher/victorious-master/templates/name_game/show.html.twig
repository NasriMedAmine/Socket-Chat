{% extends 'base.html.twig' %}

{% block title %}NameGame{% endblock %}

{% block body %}
    <h1>NameGame</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ name_game.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ name_game.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_name_game_index') }}">back to list</a>

    <a href="{{ path('app_name_game_edit', {'id': name_game.id}) }}">edit</a>

    {{ include('name_game/_delete_form.html.twig') }}
{% endblock %}
