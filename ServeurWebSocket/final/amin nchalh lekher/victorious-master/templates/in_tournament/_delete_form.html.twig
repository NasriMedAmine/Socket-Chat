<form method="post" action="{{ path('app_in_tournament_delete', {'id': in_tournament.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ in_tournament.id) }}">
    <button class="btn">Delete</button>
</form>
