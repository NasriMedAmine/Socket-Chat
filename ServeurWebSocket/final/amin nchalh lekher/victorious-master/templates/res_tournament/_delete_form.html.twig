<form method="post" action="{{ path('app_res_tournament_delete', {'idRes': res_tournament.idRes}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ res_tournament.idRes) }}">
    <button class="btn">Delete</button>
</form>
