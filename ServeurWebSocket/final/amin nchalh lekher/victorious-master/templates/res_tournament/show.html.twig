{% extends 'base.html.twig' %}

{% block title %}ResTournament{% endblock %}

{% block body %}
    <h1>ResTournament</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdRes</th>
                <td>{{ res_tournament.idRes }}</td>
            </tr>
            <tr>
                <th>Pseudo</th>
                <td>{{ res_tournament.pseudo }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ res_tournament.password }}</td>
            </tr>
            <tr>
                <th>Mail</th>
                <td>{{ res_tournament.mail }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_res_tournament_index') }}">back to list</a>

    <a href="{{ path('app_res_tournament_edit', {'idRes': res_tournament.idRes}) }}">edit</a>

    {{ include('res_tournament/_delete_form.html.twig') }}
{% endblock %}
